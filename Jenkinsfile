pipeline {

  agent any

  stages {
      stage('teste'){
         steps{
             echo "Pipeline Usando Jenkinsfile"
         }
      }
   stages {
	stage('build'){
       	  steps{
            echo "pipline Usando Jenkinsfile"
       }
    }        
  }
}
